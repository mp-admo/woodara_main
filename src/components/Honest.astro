---

---

<section class="honest">

    <div class="container">

        <div class="honest__grid">

            <div class="honest__stat">

                <div class="honest__number">
                    10<span>%</span>
                </div>

                <div class="honest__label">
                    Fixed fee on top of the factory's<br>
                    price — the same rate whether<br>
                    that price is low or high
                </div>

            </div>

            <div class="honest__content">

                <div class="honest__eyebrow">
                    <span>07</span>
                    The honest question
                </div>

                <h2 class="honest__title">
                    Aren't you just a middleman with a markup?
                </h2>

                <div class="honest__text">

                    <p>
                        We pass through the factory's price and add a fixed 10% on top of it — not folded into the number. What stays with us is which factory it is: that relationship is built across every client we work with, and it's what keeps the rate down on your next project too, not just this one. We don't gain from a bigger invoice either way — you're free to compare our price against other suppliers, and inflating one quote isn't worth losing a developer we could be working with for the next ten projects. Loyo, PARQ, SWOI, Dodo Pizza and Escape have each brought us more than one project.
                    </p>

                </div>

                <a href="#contact" class="honest__button">
                    Get a comparative quote for your project
                </a>

            </div>

        </div>

    </div>

</section>

<style>

    .honest {
        background-color: #F4F3F0;
        border-bottom: 0.05rem solid #888888;
    }

    .honest .container {
        width: 100%;
        max-width: 140rem;
        padding: 0 2rem;
        margin: 0 auto;
    }

    .honest__grid {
        display: grid;
        grid-template-columns: 35.5% 65.5%;
        align-items: stretch;
    }

    .honest__stat {
        padding: 17rem 8rem 10rem 0;
    }

    .honest__number {
        color: #151516;
        font-family: 'TT Firs Neue', sans-serif;
        font-size: clamp(10rem, 11vw, 12.8rem);
        line-height: 1.1;
    }

    .honest__number span {
        color: var(--color-accent);
    }

    .honest__label {
        color: #58585A;
        font-family: 'TT Firs Neue', sans-serif;
        letter-spacing: -0.03em;
        font-size: 1.6rem;
        line-height: 1.4;
        text-transform: uppercase;
    }

    .honest__content {
        border-left: 0.05rem solid #888888;
        padding: 10rem 0 10rem 8rem;
    }

    .honest__eyebrow {
        display: flex;
        align-items: center;
        gap: 1rem;
        font-family: 'TT Firs Neue', sans-serif;
        color: #58585A;
        font-size: 1.6rem;
        letter-spacing: -0.03em;
        text-transform: uppercase;
        margin-bottom: 6rem;
    }

    .honest__eyebrow::before {
        content: "";
        width: 2rem;
        height: 0.1rem;
        background-color: #888888;
        flex-shrink: 0;
    }

    .honest__eyebrow span {
        color: var(--color-accent);
    }

    .honest__title {
        max-width: 60rem;
        color: #151516;
        font-size: clamp(4.2rem, 4.4vw, 5.2rem);
        margin-bottom: 4rem;
    }

    .honest__text {
        max-width: 90rem;
        margin-bottom: 6rem;
    }

    .honest__text p {
        color: #58585A;
        font-size: 1.6rem;
        line-height: 1.4;
        margin: 0;
    }

    .honest__button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-family: "Outfit", sans-serif;
        font-size: 1.8rem;
        letter-spacing: -0.03em;
        padding: 1.5rem 2.7rem;
        font-weight: 400;
        border: 0.1rem solid #888888;
        background-color: transparent;
        color: #151516;
        text-decoration: none;
        cursor: pointer;

        transition: 0.3s ease;
    }

    .honest__button:hover {
        transform: translateY(-2px);
    }

    @media (max-width: 768px) {

        .honest__grid {
            display: flex;
            flex-direction: column;
        }

        .honest__content {
            order: 1;
            border-left: none;
            padding: 6rem 0 0;
        }

        .honest__eyebrow {
            margin-bottom: 4rem;
            font-size: 1.3rem;
        }

        .honest__title {
            max-width: 100%;
            font-size: 3.6rem;
            margin-bottom: 3rem;
        }

        .honest__text {
            max-width: 100%;
            margin-bottom: 4rem;
        }

        .honest__text p {
            font-size: 1.4rem;
        }

        .honest__button {
            width: 100%;
            transition: none;
        }

        .honest__button:hover {
            transform: none;
        }

        .honest__stat {
            order: 2;
            border-top: 0.05rem solid #888888;
            padding: 3.2rem 0 6rem;
            margin-top: 3.6rem;
        }

        .honest__number {
            font-size: 8rem;
            line-height: 1;
        }

        .honest__label {
            font-size: 1.3rem;
        }

    }

    @media (max-width: 1024px) {

        .honest__number {
            font-size: 8rem;
        }
    }

</style>